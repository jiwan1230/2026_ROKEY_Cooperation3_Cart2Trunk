# Single candidate placement generated during planning, before it is
# assigned a task_id/sequence and becomes a PickPlaceTask.
string box_id
geometry_msgs/Pose target_pose
float32 score
string trunk_frame_id
